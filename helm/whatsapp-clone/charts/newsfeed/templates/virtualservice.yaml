{{- if .Values.istio.enabled }}
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ include "newsfeed.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "newsfeed.labels" . | nindent 4 }}
spec:
  hosts:
    - {{ include "newsfeed.fullname" . }}
  http:
    - name: newsfeed-http
      timeout: {{ .Values.istio.virtualService.timeout }}
      retries:
        attempts: {{ .Values.istio.virtualService.retries.attempts }}
        perTryTimeout: {{ .Values.istio.virtualService.retries.perTryTimeout }}
        retryOn: {{ .Values.istio.virtualService.retries.retryOn | quote }}
      route:
        - destination:
            host: {{ include "newsfeed.fullname" . }}
            port:
              number: {{ .Values.service.port }}
          weight: 100
{{- end }}
