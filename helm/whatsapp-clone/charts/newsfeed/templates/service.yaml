apiVersion: v1
kind: Service
metadata:
  name: {{ include "newsfeed.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "newsfeed.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "newsfeed.selectorLabels" . | nindent 4 }}
