apiVersion: v1
kind: Service
metadata:
  name: {{ include "reels.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "reels.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reels.selectorLabels" . | nindent 4 }}
